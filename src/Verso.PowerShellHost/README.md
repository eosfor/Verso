# Verso.PowerShellHost

PowerShell host adapter used by Verso PowerShell notebooks.

This package contains the host integration layer that maps PowerShell host UI,
input, output, prompts, and stream behavior onto Verso notebook execution
contexts.
